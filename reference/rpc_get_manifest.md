# Get the manifest of registered functions from an RPC server

Get the manifest of registered functions from an RPC server

## Usage

``` r
rpc_get_manifest(sock)
```

## Arguments

- sock:

  A nanonext socket connected to the RPC server

## Value

A list of function signatures
