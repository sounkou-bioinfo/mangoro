# Unpack a 32-bit integer from raw bytes (big-endian)

Unpack a 32-bit integer from raw bytes (big-endian)

## Usage

``` r
mangoro_unpack_int32(bytes)
```

## Arguments

- bytes:

  A raw vector of length 4

## Value

An integer value
